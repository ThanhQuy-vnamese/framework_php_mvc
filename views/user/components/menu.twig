<!--::header part start::-->
<header class="main_menu home_menu">
	<div class="container">
		<div class="row align-items-center">
			<div class="col-lg-12">
				<nav class="navbar navbar-expand-lg navbar-light">
					<a class="navbar-brand" href="{{ redirect('') }}">
						<img src="{{ createLink('style/img/logo.png') }}" alt="logo">
					</a>
					<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>

					<div class="collapse navbar-collapse main-menu-item justify-content-center" id="navbarSupportedContent">
						<ul class="navbar-nav align-items-center">
							<li class="nav-item active">
								<a class="nav-link" href="{{ redirect('') }}">{{ translate('home') }}</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{ redirect('about') }}">{{ translate('about_us') }}</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{ redirect('user/blog') }}">{{ translate('blog') }}</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{ redirect('doctor') }}">{{ translate('doctors') }}</a>
							</li>
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" href="blog.html" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    {{ translate('services') }}
								</a>
								<div class="dropdown-menu" aria-labelledby="navbarDropdown">
									<a class="dropdown-item" href="{{ redirect('statistic-covid') }}">{{ translate('covid_statistics') }}</a>
									<a class="dropdown-item" href="{{ redirect('user/book-list-clinic') }}">{{ translate('calendar') }}</a>
									<a class="dropdown-item" href="{{ redirect('user/medican-record') }}?user_id={{ session.get('user').user_id}}">{{ translate('medical_file') }}</a>
									<a class="dropdown-item" href="{{ redirect('user/medical-healths') }}">{{ translate('health_declaration') }}</a>
								</div>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{ redirect('contact') }}">{{ translate('contact') }}</a>
							</li>
							<li class="nav-item dropdown">
								{% if(Session.get('user') != '') %}
									<a class="nav-link dropdown-toggle" href="blog.html" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
										<i class="fa-solid fa-user"></i>
										{{ session.get('user').first_name}}
									</a>
									<div class="dropdown-menu" aria-labelledby="navbarDropdown">
										<a class="dropdown-item" href="{{ redirect('user/profile')}}?user_id={{ session.get('user').user_id}}">{{ translate('profile') }}</a>
										<a class="dropdown-item" href="{{ redirect('user/logout') }}">{{ translate('log_out') }}</a>
									</div>
								{% else %}
									<a class="btn_2 d-none d-lg-block" href="{{ redirect('user/login') }}">{{ translate('login') }}</a>
								{% endif %}
							</li>
                            {% if Auth.user().role == 1 or Auth.user().role == 2 %}
                                <li class="nav-item">
                                    {% if Auth.user().role == 1 %}
                                        <a href="{{ redirect('admin/user-list') }}"><i class="fa-solid fa-gear fa-xl"></i></a>
                                    {% elseif Auth.user().role == 2 %}
                                        <a href="{{ redirect('admin/calendar') }}"><i class="fa-solid fa-gear fa-xl"></i></a>
                                    {% endif %}
                                </li>
                            {% endif %}
							<li class="nav-item"></li>
						</ul>
					</div>
				</nav>
			</div>
		</div>
		<div class="row align-items-center mb-5">
			<div class="col-lg-12">
				<form method="GET" action="{{ redirect('user/search-doctor') }}">
					<div class="input-group rounded">
						<input type="search" name="keyword" class="form-control rounded" placeholder="{{ translate('search_doctor') }}" aria-label="Search" aria-describedby="search-addon"/>
						<span id="search-addon" style="padding-left:10px">
							<input class="btn btn-info" type="submit" value="{{ translate('search') }}"/>
						</span>
					</div>
				</form>
			</div>
		</div>
	</div>
</header>
<!-- Header part end-->
